[variables]
    NIXPACKS_PYTHON_VERSION = "3.12"

[phases.setup]
    nixPkgs = ["python312", "python312Packages.pip", "nodejs_20", "pkg-config", "libGL", "glib", "tesseract"]

[start]
    cmd = "python3 server_main.py"
