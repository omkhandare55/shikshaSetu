[variables]
    NIXPACKS_PYTHON_VERSION = "3.12"

[phases.setup]
    nixPkgs = ["python312", "pkg-config", "libGL", "libglib"]

[phases.install]
    dependsOn = ["setup"]
    cmds = ["pip install -r requirements.txt"]

[start]
    cmd = "python3 server_main.py"
